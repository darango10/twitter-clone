import React from 'react';
import '../styles/widgets.css'

const Widgets = () => {
    return (
        <div className="widgets">
            <h1>We're the widgets</h1>
        </div>
    );
};

export default Widgets;
